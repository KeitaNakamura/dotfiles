# PATH
export PATH="/usr/local/sbin:$PATH"

# use color
autoload -Uz colors
colors

# for wine
# export LC_ALL=ja_JP.UTF-8

# vim
# bindkey -v

# complete
fpath=(/usr/local/share/zsh-completions $fpath)
autoload -U compinit
compinit

setopt auto_pushd
setopt share_history
setopt ignore_eof

# copy corrent directory path
alias cpwd="pwd | tr -d '\n' | pbcopy"
